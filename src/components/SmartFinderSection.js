
import React from 'react';
import SmartFinder from './SmartFinder';

const SmartFinderSection = ({ groceryItems }) => {
  return <SmartFinder groceryItems={groceryItems} />;
};

export default SmartFinderSection;
